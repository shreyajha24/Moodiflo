# Backend API Base URL (leave blank when using same-origin / reverse proxy, or set to deployed backend API URL)
# Production backend origin. API services append paths such as /api/spotify/search.
VITE_API_BASE_URL=https://moodiflo-backend.onrender.com
